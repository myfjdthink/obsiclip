细节体验调整一下

- 用户没有配置 API KEY 的情况下，点击 AI 智能整理，应该主动引导用户去填写 API KEY
- 配置页面，宽度不够，很拥挤，不知道为什么一定要在插件信息页面弹出 配置 Popup，可能的话在 SIdebar 里完成配置会更好
- 推荐的模型，也要根据 AI 服务商变化

----------
配置页面

压缩 API KEY 配置组件的高度，不要占太多空间
留开更多空间给 Prompt 设置组件，用户才能方便地看清楚 Prompt

我尝试保存 Deepseek API KEY，页面提示保存失败

-----------
这是我之前用来处理文章的 Prompt，/Users/nick/note/AI-Note/Claude.md 
里面讲了怎么给文章分类和总结文章，把这里的分类和总结的要求更新到插件的默认 Prompt 里

我希望 AI 可以帮我做到的事情有：
- 自动重新生成标题，讨厌哪些标题党和震惊体，要根据内容生成一个读者能快速理解的标题
- 生成文章分类，Prompt 里会给出分类的选项，没有则留空
- 识别作者

这样就要求 AI 返回 Structure Data，你看看有没有必要时用 Vercel AI SDK ，可以方便地返回 JSON

----------
页面交互细节要相应地调整
- TAB“AI预览” 改名为 “AI整理”
- AI 可以流式返回内容，先返回整理后的标题，分类这些信息，然后返回正文，正文还要用 https://github.com/Simon-He95/markstream-vue 这个工具来流式渲染
- 修改 Prompt，TAG 标签可以不用 AI 来整理，页面上也移除这个东西
- 移除 Vault 输入框


-------------
继续调整

Prompt 要分为系统设定的部分和用户自定义的部分

像输出格式，应该是定死的，用户不会修改到的

能修改的就是分类方式和内容整理的方式

所以最终 Prompt 是两者组合出来

------------
我说要实现的效果

点击 AI 智能整理 后，标题，文件夹路径（分类）会自动填写（AI 返回的）
正文会流式加载出来，用户不用长时间等待

现在还没做到，你看看要如何实现

--------
不应该在代码里对类别做映射  CATEGORY_FOLDER_MAP， 类别是用户在 Prompt 里定义的，AI 返回是什么就是什么

另外，现在Title 和类别都会自动填写了，但是正文这里，还是要等所有内容加载完了才出现，不会有流式渲染的效果，就是那种打字效果，看看哪里有问题



------------
- 用户首次打开插件，现在就进入配置页面，这个体验不对，应该是用户点击 “AI 智能整理” 后，检查有没有 KEY，没有才进入配置页面
- 标签这个输入框已经不用了，请移除
- 文件夹路径改名叫“文件夹/类别”
- “文件夹/类别” 和保存按钮放在同一行，并列
- 正文渲染的时候，我希望用户能一直看到最新内容，现在只能看到滚动条一直在变小（因为默认在看前面的内容）


------
“文件夹/类别” 这个输入框是通过 placeholder 来提示用户这是什么的，那么 AI 自动填写值之后，用户就不知道这个东西是干嘛的了，怎么优化？

--------
先隐藏 “手动调整选取” 和遮罩功能，这个功能后面再迭代，现在自动推测的效果已经足够好了